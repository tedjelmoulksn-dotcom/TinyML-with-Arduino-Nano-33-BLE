## Why Node-RED Is Used

Node-RED is used to handle the counting and visualization part of the project.

The Arduino performs the image classification and sends the detected class through the serial port.

Node-RED receives these messages, updates the corresponding counters, and displays the results on a dashboard.

This makes the system easier to monitor and provides a clear real-time view of the detected components.
